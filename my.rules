((for *
      (when *
        (slack :webhook "http://example.com/..."
               :text "$topic is $status"
               :color (if ok? "good" "danger")
               :attach (if link
                           "$message <$link>"
                           "$message")))))
